services:
  - type: web
    name: edufocus-system
    env: node
    plan: free
    buildCommand: bash build.sh
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20.x
